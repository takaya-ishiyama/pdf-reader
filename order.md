# 設計書作成依頼
## 要求
設計書を作ってほしい

## 概要
このアプリはmarkdownをアンドロイドアプリで表示するためのものです。以下に設計書の概要を示します。

## 使用する技術
- apiがrustで作成
- dbはpostgresql
- dbサーバーはneon
- androidアプリはkotlin
- apiのサーバーはgcp
- markdown用のストレージはgcs

## 詳細な要求
設計をし、その設計内容をmarkdownファイルにまとめてほしい。
前提条件として認証は不要。あとから追加する可能性はあるが、現段階では考慮しなくて良い。
まず、api側はcloud runにデプロイしている。まだ設定はしていないが、makdownをgcsに配置するつもりだ。
apiはDBからそのmarkdownの名前とversion、ユーザーが最後どこまで読んだのかを持っておく。そして、gcsの著名付きURLを返すようにする。
markdownは更新する可能性があるためバージョンの概念をもたせる事。また、markdownの更新の頻度はかなり低い。そのため、キャッシュ(httpキャッシュなのか、ローカルキャッシュなのかgcsのキャッシュなのかなど)をもたせる事。
DBマイグレーションはatlasを使用する。sqlライブラリはsqlxを使用する。
postgresのスキーマはpublicではなくpdf_readerにし、権限もpublicではなく適切にロールを作成すること。
また、アプリ側から最後に読んだ部分を変更するAPIを用意する。このAPIは普通のRESTにするのかwebsocketにするのか、どのタイミングで送信するのかなどあまり考えられていないため詳しく設計してほしい。
markdown自体のアップロード機能は不要。
androidアプリ側はmarkdown情報をもらい、著名付きurlを使用し、gcsからmarkdownを取得する。そして、取得したmarkdown情報から、api設計で記述した、最後に読んだ部分を記録するapiを叩く設計は方法が色々あり、メリットデメリットがあると思うので考えてほしい
また、そのmarkdownの読みあげ機能も欲しい。
